# 資料庫（部署用 PostgreSQL，到 https://neon.tech 建立免費專案取得）
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-random-secret-at-least-32-chars

# Stripe（測試模式用 pk_test_ 和 sk_test_ 開頭的 key）
# 到 https://dashboard.stripe.com/test/apikeys 取得
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxx
STRIPE_SECRET_KEY=sk_test_xxxxx

# 訂閱方案：到 Stripe Dashboard → Products → 新增 Product → 新增 Price $10/月 → 複製 Price ID
STRIPE_PRICE_ID=price_xxxxx

# 選填：Webhook 簽章（正式版用）
# STRIPE_WEBHOOK_SECRET=whsec_xxxxx

# Vercel Cron：部署到 Vercel 時在專案設定新增 CRON_SECRET，Cron 會帶此 header 驗證
# CRON_SECRET=your-cron-secret
